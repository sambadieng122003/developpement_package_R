% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_poverty_distribution.R
\name{plot_poverty_distribution}
\alias{plot_poverty_distribution}
\title{Plot Poverty Distribution by Group}
\usage{
plot_poverty_distribution(
  result,
  indicator = "share_poor",
  save = FALSE,
  filename = "distribution_plot.png"
)
}
\arguments{
\item{result}{A \code{data.frame} returned by the function \code{poverty_distribution()}.}

\item{indicator}{A character string specifying the indicator to plot:
must be either \code{"share_of_poor"} or \code{"share_of_population"}.}

\item{save}{Logical. If \code{TRUE}, saves the plot as a PNG image. Default is \code{FALSE}.}

\item{filename}{A character string specifying the name of the PNG file if \code{save = TRUE}.}
}
\value{
A \code{ggplot} object representing the selected poverty distribution measure. The function also prints the plot to the viewer.
}
\description{
This function visualizes the distribution of poverty across groups,
using the output of \code{poverty_distribution()}. It can display either the
share of poor or the share of population by group.
}
\details{
The first column of \code{result} is assumed to be the grouping variable (e.g. region, gender, etc.).
If the \code{Total} row is present in the result, it will be excluded from the plot.

The plot is rendered as a horizontal bar chart with percentage labels directly on each bar.
}
